describe('Admissions Reducer', () => {
  describe('an unknown action', () => {
    it('should return the previous state', () => {


      // const result = reducers(initialState, action);

      // expect(result).toBe(initialState);
    });
  });
});
